// A deliberately boring module. Its only job is to give pull requests
// something to conflict over.
export const VERSION = '2.0.0-from-branch';
export const greet = (who) => `hello, ${who}`;
export const add = (a, b) => a + b;
